@model UsersListViewModel

@if (Model.Users.Count > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Name</th>
                <th>Surname</th>
                <th>Email</th>
                <th>Phone number</th>
                @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                {
                    <th>Actions</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model.Users)
            {
                <tr>
                    <td>@user.FirstName</td>
                    <td>@user.LastName</td>
                    <td>@user.Email</td>
                    <td>@user.PhoneNumber</td>
                    @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                    {
                        <td>
                            <button class="btn btn-sm btn-warning" onclick="editClient('@user.Id')">Edit</button>
                            <button class="btn btn-sm btn-danger" onclick="showDeleteClientModal('@user.Id')">Delete</button>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p class="lead text-secondary fw-bold mt-3">No clients found!</p>
}